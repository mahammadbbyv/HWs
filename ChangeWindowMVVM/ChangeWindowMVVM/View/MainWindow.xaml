<Window x:Class="ChangeWindowMVVM.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChangeWindowMVVM"
        xmlns:ViewModel="clr-namespace:ChangeWindowMVVM.ViewModel"
        d:DataContext="{d:DesignInstance Type=ViewModel:MainViewModel}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.2*"/>
            <RowDefinition Height="0.8*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Button Content="1" Width="20" Margin="10" Command="{Binding ChangeCommand}" CommandParameter="1"/>
            <Button Content="2" Width="20" Margin="10" Command="{Binding ChangeCommand}" CommandParameter="2" />
            <Button Content="3" Width="20" Margin="10" Command="{Binding ChangeCommand}" CommandParameter="3" />
        </StackPanel>
        <ContentControl Grid.Row="1" Content="{Binding CurrentViewModel}"/>
    </Grid>
</Window>
